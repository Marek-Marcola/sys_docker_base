ARG FROM
FROM $FROM

ARG DATE
ARG FROM
ARG REPO
ARG VER

LABEL info.from="$FROM"
LABEL info.name="$REPO:$VER"
LABEL info.date="$DATE"

ENV INFO_DATE=$DATE INFO_REPO=$REPO INFO_VER=$VER INFO_FROM=$FROM
ENV PATH /usr/local/dovecot-$VER/bin:/usr/local/dovecot-$VER/sbin:$PATH

RUN set -ex;\
  if [ "$(echo $OS_NAME|cut -b 1-6)" = "debian" ]; then\
    export DEBIAN_FRONTEND=noninteractive;\
    apt-get update;\
    apt-get -yqq --no-install-recommends install libicu72 liblua5.4;\
    apt-get clean;\
  fi;\
  groupadd -g 5000 vmail;\
  useradd -g 5000 -u 5000 -m vmail;\
  mkdir -pv /var/opt/vmail;\
  chown vmail:vmail /var/opt/vmail;\
  chmod 770 /var/opt/vmail;\
  groupadd -g 850 dovecot;\
  groupadd -g 851 dovenull;\
  useradd -u 850 -g 850 -m -c "" dovecot;\
  useradd -u 851 -g 851 -m -c "" dovenull;\
  mkdir -pv /usr/local/etc/dovecot;\
  curl -sS -L -k -f http://repo/sw/opensource/dovecot/bin/dovecot-$VER-$OS_NAME-$OS_ARCH.tar.gz | tar xzf - -C /usr/local;\
  chown -R root:root /usr/local/dovecot-$VER;\
  S=zlocal-dovecot.sh;\
  curl -sS -L -k -f -o /etc/profile.d/$S http://repo/pkb/pb/playbooks/101008-mail_dovecot/files/$S\
    && (chmod a+x /etc/profile.d/$S)\
    || (echo Script $S not available);\
  dovecot --version;

RUN set -ex;\
 (echo "info.date = $DATE";\
  echo "info.name = $REPO:$VER";\
  echo "info.from = $FROM";\
 ) > /version.d/version-$REPO.txt;

ADD scripts/[0-9]*.sh /cmd.d/
CMD ["/cmd.sh"]
